<?xml version="1.0" encoding="UTF-8"?>
<!-- Database logging : Appender for streaming logs to Db
Note => 1. Create the Log table using the logback script that matches your Db
e.g. MySql -> logback-classic/src/main/java/ch/qos/logback/classic/db/script/mysql.sql
 2. For performance reasons, it's better and advisable to use Datasource or JNDI
-->

<included>
    <appender name="DB_APPENDER" class="ch.qos.logback.classic.db.DBAppender">
        <connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource">
            <driverClass>com.mysql.cj.jdbc.Driver</driverClass>
            <url>jdbc:mysql://localhost:3306/classicmodels</url>
            <user>root</user>
            <password>toor1234</password>
        </connectionSource>
    </appender>
</included>
